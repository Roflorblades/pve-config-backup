# Proxmox VE Config Backup

Schlankes Bash-Skript zur Sicherung von PVE-Konfigurationsdateien (`/etc/pve`, Netzwerk, Corosync) auf einen externen VPS.

## Einrichtung

1. Skript ausführbar machen:
   ```bash
   chmod +x backup.sh
   ```